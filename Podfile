# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'buysell-azusapacific' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for buysell-azusapacific 
      pod 'Firebase'
      pod 'Google/SignIn'
      pod 'Google/Analytics'

  target 'buysell-azusapacificTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'buysell-azusapacificUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
